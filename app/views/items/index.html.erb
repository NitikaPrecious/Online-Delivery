
<h1>All Items</h1>

<table>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.name %></td>
        <td><%= item.description%></td>
        <td><%= item.price%></td>
        <td><%= link_to "Show", item %>  </td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>